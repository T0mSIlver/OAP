The CPLEX project is composed of 2 running configurations : 
	-mod1 : the model where the decision variable is a floating point number
	-modINT : the decision variable in an integer and the objective function takes the fabric waste into account.

Both models use the same data.

Author : VAUCOURT Tom